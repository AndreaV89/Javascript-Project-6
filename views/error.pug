extends layout.pug

block content
    article.grid-container.portfolio-intro
        div
            h1= `${error.message} - Status: ${error.status}`
            pre= error.stack
            a.button(href='/') Return Home